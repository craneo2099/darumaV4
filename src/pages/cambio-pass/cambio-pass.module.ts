import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CambioPassPage } from './cambio-pass';

@NgModule({
  declarations: [
    CambioPassPage,
  ],
  imports: [
    IonicPageModule.forChild(CambioPassPage),
  ],
})
export class CambioPassPageModule {}
